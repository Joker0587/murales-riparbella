{
  "installCommand": "rm -rf node_modules package-lock.json pnpm-lock.yaml && yarn install --non-interactive --network-timeout 120000 --registry https://registry.npmjs.org",
  "buildCommand": "yarn build",
  "outputDirectory": "dist",
  "framework": "vite"
}
